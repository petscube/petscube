import { LocationSearchDirective } from './location-search.directive';

describe('LocationSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationSearchDirective();
    expect(directive).toBeTruthy();
  });
});
